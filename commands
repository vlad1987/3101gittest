git init
git status

git config --global user.name "Mike Anderson"
git config --global user.email "mike@site.com"

git add <file>
git commit -m "<message>"

git log 
 - вниз = Enter
 - выйти = q
 
 git config --global alias.tree "log --oneline --decorate --all --graph"
 git tree

 git diff